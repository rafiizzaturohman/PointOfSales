<% if(error.length> 0) {%>
    <div>
        <h1 class="text-center text-red-500">
            <%= error %>
        </h1>
    </div>
    <% } %>

        <% if(success.length> 0) {%>
            <div>
                <h1 class="text-center text-green-600">
                    <%= success %>
                </h1>
            </div>
            <% } %>